# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Native bridge — ship only the shared dylib in wheels, not static archives
src/apple_fm_sdk/lib/*.a
\n# Local notes\nnotes/
